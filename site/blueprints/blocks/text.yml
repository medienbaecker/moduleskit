name: Text
preview: fields
wysiwyg: true
icon: text
tabs:
  content:
    fields:
      headline:
        type: text
      textarea: true
  settings:
    fields:
      background:
        type: select
        options:
          light: Light
          dark: Dark
