title: Images
icon: image

fields:
  gallery:
    label: Images
    type: files
    # `model.filePool` makes the picker show files from the
    # (grand)parent page instead of the module's own files.
    query: model.filePool
    uploads:
      parent: model.filePool.parent
    layout: cards
